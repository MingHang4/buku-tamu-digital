name: Demo Success

on:
  push:
    branches: [ main ]

jobs:
  demo-success:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Demo success
        run: |
          echo "This job runs successfully for demo purposes"
          echo "Node version: $(node --version || echo 'node not available')"
          echo "Npm version: $(npm --version || echo 'npm not available')"
          echo "Demo workflow completed successfully"
